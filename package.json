{
  "name": "vite-ts-lib-frame",
  "version": "0.0.2",
  "type": "module",
  "scripts": {
    "dev": "yarn l && yarn dev:example",
    "dev:example": "yarn --cwd example dev",
    "l": "yarn --cwd lib link && yarn --cwd example link '@inner/lib'",
    "ii": "yarn && yarn --cwd example",
    "build": "vite build && tsc",
    "preview": "vite preview"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^18.8.3",
    "typescript": "^4.6.4",
    "vite": "^3.1.0"
  },
  "files": [
    "dist"
  ],
  "main": "dist/index.js"
}
